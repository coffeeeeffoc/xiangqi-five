# 象五子棋 · 第三轮 X3 缩放回归整改 · 2026-09-22

本轮修复新的独立 R2 扣分项：真实有头 Chrome 153 的鼠标/触摸放大后会从 150% 自动回到 100%。产品变更只涉及 **两处 CSS**：删除第二轮短横屏的 `scrollbar-gutter:auto` 覆盖、在 `html` 预留稳定滚动条空间。保留原有 `board-view.js` 及全部输入/规则逻辑，仍由实际容器尺寸变化、恢复按钮、建盘和显示模式事件重置缩放。无提交、部署或实现者评分上调；后续协调者正常打包38/38、最新HTTP三入口3/3已通过，新R3独立评审确认X3关闭，原值8.750，仍未达9.5。详见 `INDEPENDENT-REVIEW.md` 顶部和根计划。

## 根因与修前证据

- 本轮使用评审给出的 `chromium.launch({channel:'chrome',headless:false})`、844×390、`isMobile:false/hasTouch:true`，在未修改的 43142 独立入口稳定复现：初始 `clientWidth/offsetWidth=278/278`，放大后垂直滚动条出现变为 `263/278`，`ResizeObserver` 把减少的 15px 当作真实布局变化，调用 `reset()`，又回到 `278/278` 和 100%。连续四次鼠标点击均为 `100→150→100`。
- 自有 `browser-check.mjs` 新增等候两帧的缩放断言，修前在第一个桌面鼠标用例直接失败：预期 150%，实际 100%。之前只用无头移动环境，没有经典滚动条，也没有在观察器结算后持续检查中间缩放值，漏掉了此回归。
- 删除短横屏覆盖后继续检查兄弟路径：转到 305×740 再放大仍失败，整页滚动条改变了页面可用宽度（当时 `client=259/outer=274/page=290/window=305`）。所以同时为根页面预留滚动条空间；不在每个按钮旁规避或禁用正常尺寸重置。
- 最初按转交的最小命令重跑了评审脚本，它会写回自己目录的 JSON/PNG；已告知协调者和评审者，后者自行重跑固定旧制品恢复独立证据归属。未修改独立报告/冻结制品，后续截图只写本子模块自己的已忽略目录 `.turbo/review-r3/`。

## 修后当前证据

- `pnpm build`：通过。设置 `XIANGQI_HEADED=1`、`XIANGQI_SHELL_REVIEW_URL=http://127.0.0.1:43142/small-games/#/games/xiangqi-five` 后运行 `node browser-check.mjs`：**有头 Chrome 153.0.8010.50，38 组通过**。
- 未设置 `XIANGQI_HEADED`、使用同一个 Shell 测试入口再运行 `node browser-check.mjs`：**无头 Chromium 140.0.7339.186，38 组通过**。两种环境均实际运行了桌面鼠标、桌面触摸和移动触摸配置，没有把无头移动结果代替有头结果。
- 每种输入配置覆盖 9×10、15×15、独立 844×390/844×334 和实际 Shell：全局面可见，首末两格落子；逐次放大后的稳定值是 150/200/250/300%，内容宽度不变；300% 下实际选中中部格落子；CDP 双指 `touchStart→touchMove→touchCancel` 不产生棋步，取消保护期后继续落子；保持四手棋局转到 305×740，正常恢复适屏，竖屏再次放大/恢复及刷新续局全部通过。
- 同一回归还重验原有全屏/退出/降级、误触不耗步、抽子可见及存档、三档真实 Worker、胜负重试、房间断网中文提示/恢复/退房还原。规则算法本轮未改；本轮新增验证是上述浏览器矩阵，上一轮的 20 项规则结果保留在历史记录中。`git diff --check` 通过。
- Shell 检查仍是未修改的 43142 页面加**测试上下文内**当前游戏资源覆盖，常规打包后的 HTTP 制品须由协调者再次验证。未声称这是新线上部署或 Android/iOS 真机结果。

有头截图与逐步宽度数据：`.turbo/review-r3/standalone-headed-300.png`、`.turbo/review-r3/shell-headed-300.png`、`.turbo/review-r3/zoom-green.json`。该数据来自额外一次正常鼠标点击：独立入口从 100% 到 300% 的 `clientWidth` 始终 **263**、`offsetWidth` 始终 **278**；Shell 内分别始终 **207/222**。截图中 300% 是放大后的局部棋盘，恢复 100% 的全局面可见另有矩阵断言。

当前制品 SHA-256：`dist/style.css` = `14b167e61145a9d32da5cc8251aeb14232f572aee9a07b22e10e95647765efd9`；未改动的 `dist/board-view.js` = `ea79c1669a9e36b5ca3522269f3bfd690d83146d49a6137eb07b058ebfbe4c18`；`dist/app.js` = `cf63c2702bcf3ba2d79201c9ffdfcb1cc026f080fa1e2bd62ecfe9cc946c8884`。产品文件已冻结；完整发布文件树摘要 `80048623dd600b122cdcdd55edbd69ad9616cc590f3d0e3cff7fa565011f8ca2`。新独立R3的16组显示/输入、4组五连重开及4组实际桌面全屏通过；真机、目标玩家和公网房间的既有证据缺口没有被这次代码修复消除，保持未达标。

---

# 第二轮归档：整改交付 · 2026-09-22

本节覆盖新独立评审发现的两个可复现问题。仅修改本子模块的 `style.css`、`app.js`、`browser-check.mjs` 和本报告；未改评审快照、`INDEPENDENT-REVIEW.md`、共享全屏控制器或根配置。产品代码已冻结，等待新一轮独立复评。以下改善不自动转化成 9.5 分，Android/iOS 真机、公网服务和真人证据仍待验证。

| 独立扣分项 | 修前复现 | 本轮最小修复与玩家收益 | 已验证的通过条件 |
| --- | --- | --- | --- |
| 短横屏满宽棋盘无法总览 | 新增检查修前失败：844×390 独立象棋盘为 `(x18,y295,w808,h897.765625)`；原样只读 43141 聚合快照的 15×15 为 `(x18,y351,w808,h808)`，首屏仅 39px 棋盘可见。 | 在已有小屏布局增加短横屏 CSS Grid，控件在左、棋盘在右，棋盘宽度由当前视口高度和棋盘比例确定；原有 100%–300% 缩放保留。两种方向可自由切换，不强制横屏、不改变棋局。 | 844×390、844×334 两棋盘首屏完整棋盘；先点击首末两格、放大到 300% 再点中部，实际手数为 3；旋转至 305×740 并刷新保持三手、无横向溢出。 |
| 好友房断网暴露英文底层错误 | 两个真实浏览器页面连入本地 API 后将红方浏览器设为 offline，新增检查修前读到 `3753CFB7 · 你执红 · Failed to fetch`。 | 在共用 `roomRequest` 的 fetch 边界统一连接/超时中文反馈，覆盖轮询、行动和连接调用；有房间时显示“连接中断，正在重试同步。”，尚未入房时说明检查网络后重试。保留服务端业务错误与原有锁定/重试；恢复时清理弹窗内旧断网提示。 | 断网后棋盘锁定且状态条、房间提示都为中文；恢复网络后操作解锁并显示“连接已恢复，棋局已同步。”；继续原有刷新恢复席位、取消重开、退房还原本机棋局检查。 |

验证基于当前重新构建的 `dist/`，不是截图判断可玩。`pnpm build` 成功；`pnpm test` **20/20**；默认 `node browser-check.mjs` 覆盖 **24 组**，本轮实际设置 `XIANGQI_SHELL_REVIEW_URL=http://127.0.0.1:43141/small-games/#/games/xiangqi-five` 运行，合计 **26 组全部通过**；`git diff --check` 通过。

追加的两组 Shell 检查使用原样 43141 Shell 页面，仅在该独立浏览器上下文拦截 `/small-games/games/xiangqi-five/*` 并返回本轮 `dist/` 对应资源：两棋盘横屏全部行在外层 390px 内，首末格触摸及转回 305px 后继续落子通过。没有改写冻结快照或其他代理的证据；该结果是新资源与现有 Shell 的布局集成检查，协调者仍需常规打包后实际 HTTP 复验。上述旧版红灯检查没有开启请求覆写。

当前回归同时重验第一轮 305px 误触/抽子/续局、全屏与退出、受控手势取消、三档 Worker、红黑结算重试及双页房间流程。可见 Chrome CUA 在新源码 `http://127.0.0.1:43135/`、844×390 下确认 15×15 全部棋盘可见，实际点击 A1 与 O15 后记录为 2。浏览器视口已复原。这些均不冒充实体触屏或 iOS/Android 验收。

本轮更新制品 SHA-256：`dist/app.js` = `cf63c2702bcf3ba2d79201c9ffdfcb1cc026f080fa1e2bd62ecfe9cc946c8884`；`dist/style.css` = `25fd1de526ee3138a57cb3b511f1e869f3271191fc1021324cb80bd94752fc85`。其余游戏产品文件未由本轮修改；以新 hash 替代下方第一轮对应文件 hash。后续公共控制器/打包变化仍须重新核验。

尚存边界：短横屏完整局面里的 15×15 棋格较小，需要放大进行精确操作；已验证 300% 浏览器触摸坐标，真人误触率和实体手机手势仍未知。没有连续新收益前不会再添加装饰性功能抬分；暂不改第一轮自评数字，以最新独立评审为准。

---

# 第一轮归档：9.5 冻结量表实现记录 · 2026-09-22

本节是当前轮次，后文保留旧报告作历史。实现者 `/root/implement_chess`；独立评审尚未完成。基于子模块 `bd8f8b7e079e6b61c6634cf4a0c0730d43765cf4` 加当前未提交改动，不沿用旧报告分数。主仓库冻结量表见 `docs/plans/2026-09-22-six-games-95.md`，9.5 不等于真实用户均分，也不等于爆款。

## 当前发布范围与实际入口

- 目录 `games/submodules/xiangqi-five/` 是独立 Git 子模块。本轮开始仅存在协调者生成的未跟踪 `fullscreen.js`，已保留；无提交、推送、部署、新依赖或服务采购。
- 已有目标是 H5 手机浏览器、桌面 Web、父仓库 Shell 同源 iframe。`pnpm build` 产出 `dist/index.html`；源码加本地房间 API 的预览为 `http://127.0.0.1:43135/`。浏览器回归每次自启随机端口纯静态服务器，始终测试本轮 `dist/`，独立第二服务用于房间验收。公开 Pages 是已有发布入口，本轮没有部署和重验线上版本。
- 无已实现的微信/B站/其他原生小游戏入口或 SDK，不自行新增目标平台。静态单人/同屏离线规则不需要服务器；电脑使用随包 Worker。联机需要独立 Node HTTPS 房间 API；本地双页成功不能替代公网、跨运营商验收。
- 核心玩法是随机抽取象棋棋子，以各棋种走法移动、吃子，争取任意同色横竖斜连五。适合愿意尝试轻策略的象棋/五子棋玩家及朋友对弈。当前实际已有三档电脑，旧报告的“两档/有限一手评分”已过时；本轮没有改搜索器，也不为既有功能新增分数。

## 新基线与本轮实现者暂评

基线来自本轮构建、可见 Chrome 实玩、两棋盘触控回归和当前规则检查；不是旧评分复制。权重依次 30% / 20% / 20% / 15% / 15%，原值判断。

| 维度 | 新基线 | 当前 | 本轮依据与仍扣分项 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 8.6 | 8.7 | 亲自部署、电脑回应、选子与误触，回归通过两棋盘合法移动/红黑五连；修复“选择移动后意外部署”恢复玩家意图。没有真人早中后局乐趣观察，重复局面仍仅可协商重开。 |
| 首局理解与教学 | 8.0 | 8.5 | 手机抽子后隐藏标题及预览，原来无法看到抽到的棋种；现在现有按钮直接显示棋种，刷新保持。规则和选子走法可找到，仍缺真人无解释首局完成及迁移观察。 |
| 移动操作、视听与反馈 | 7.6 | 8.8 | 原无全屏；现有全屏/退出入口、305px 两棋盘、844×390 切换及继续操作检查通过，拒绝/不支持反馈可玩。15×15 仍需要缩放；未验证 Android/iOS 真机、低性能、安全区和工具栏变化。 |
| 重玩或学习价值 | 8.0 | 8.0 | 两种棋盘、三档电脑、随机棋池、同屏和本地房间带来变化；没有为未改项目加分。缺真人跨局/跨日复玩与策略学习证据，暂无残局或复盘教学的需求证据。 |
| 功能完整性与稳定性 | 8.6 | 8.9 | 新误触回归先失败后通过；20 项规则/AI/存档/HTTP 与实际双页断线/恢复/退出还原检查通过。完整公开发布路径、原生平台和实体设备未验收。 |
| 加权原值 | **8.190** | **8.605** | **未达 9.5；并非独立验收分。** |

无真实玩家样本，无法可靠给出上线后用户评分区间、留存或分享转化预估。不能凭上述代理体验推算真实满意度。

## 本轮前三问题、修改收益与通过条件

1. **明确选择“移动场上棋子”后误点空位，竟然自动抽子部署并换手。** 可见 Chrome 在第 3 手前复现，点移动后点 A1 出现红帅并进入电脑回合；新增 305px 回归修复前实际手数 3、预期 2。根因是 `play()` 快捷部署分支忽略 `moving`。现在入口和可部署高亮统一检查 `!moving`，误触给出“先点己方棋子”提示。通过条件：两棋盘误触后手数/存档字节不变，再选自己的棋子和合法位置仍能移动；本轮已通过。
2. **手机抽子后看不到棋种。** 手机 CSS 隐藏 `draw-preview` 与 `action-title`，原按钮只显示待部署，影响玩家根据走法选落点。现在复用现有按钮显示“已抽到「马」· 点空位”，不增加弹窗和信息区。通过条件：305px 两棋盘抽子显示实际棋种；刷新后不重抽，信息仍可见，合法部署完成；已通过。可见 Chrome 正常随机抽马后也确认可见。
3. **H5 没有全屏入口。** 在原顶栏接入协调者维护的 `fullscreen.js`，源码服务器和静态构建均包含；顶栏保持可见、按钮至少 44px、尊重安全区。完整文档包括棋盘/HUD/控件一起请求全屏，弹窗沿用公共退出按钮。显示变化只重置缩放手势，清理被打断的点击抑制，不重建棋局。通过条件与结果见下一节。

## 本轮实际验证

- `pnpm test`：**20/20 通过**。两棋盘全部规则、三档电脑防五/避免白送/预算回退/合法性、抽子锁定、存档重放，以及本地 HTTP 鉴权、版本冲突、双方重开。不是旧 13 项结果。
- `pnpm build`：通过。`node browser-check.mjs`：**20 组通过**，基于每次新建静态产物服务器；保留脚本作为回归。无 TypeScript 工程，不能虚报类型检查。
- 当前制品 SHA-256（2026-09-22，供后续集成比对）：`dist/index.html` = `045a7a43c475c38e793bf5493d246408793f1d1ae9081519a536a3ec722e4ebe`；`dist/app.js` = `847c32a581e63f35b3075493a4009aa383529309a89ca9614427359b4f08e795`；`dist/board-view.js` = `ea79c1669a9e36b5ca3522269f3bfd690d83146d49a6137eb07b058ebfbe4c18`；`dist/style.css` = `21399ecbc4c8a44872e9e6af73d423d41f54e2d2727f92373900db894aaf5286`；`dist/fullscreen.js` = `4451eded9653669ae98fd27e9dc3eff43d41c14286ec66a27d99629f18a34828`。后续公共控制器变化需重新同步/构建，不能沿用旧 hash。
- 305×740 与 305×568 两棋盘：移动模式误点空位不消耗手数/棋池，选择后合法移动；待部署棋种可见、刷新不重抽；全屏 API 真进入/退出、弹窗内退出、外部 API 退出状态同步、844×390 旋转、缩放后真实点击继续落子；受控手势中断检查不再永久吞点击。最后一项是事件注入异常检查，不冒充真手势/真机。
- 320/390/1280 宽 × 两棋盘 6 组：相邻格下缘触摸仍准确落点、存档刷新、无横向溢出。
- 390px 两棋盘 × 三难度 6 组：真实 Worker 回应、难度刷新保存、避免部署在红车直攻线上，归还回合并解锁。另实测思考中切同屏，旧 Worker 不得向新棋局落子。
- 390px 正常动画：默认单人回应、刷新续局、静态无 API 时房间禁用并说明；真实点击同屏 9 手红方五连/5 子高亮/再来一局，15×15 放大 200% 后点击与恢复；另外 15×15 全屏中真实点击 10 手黑方五连、重试、退出全屏。该有意不防守棋谱验证结算路径，不证明电脑胜率或长期平衡。
- 新建两个浏览器页面通过真实本地 API：创建/加入、双方各落一手同步；模拟浏览器断网后锁定操作、恢复网络后重新同步；同标签刷新恢复席位/手数；重开申请由另一方取消后保留棋局；离开房间恢复入房前本机第 1 手。
- 可见 Chrome CUA：305px 真实界面重新验证误触手数不变、提示明确，正常随机抽出的马在按钮显示；已续 4 手棋局后继续部署并见电脑响应。其全屏请求被浏览器拒绝，出现“浏览器未允许全屏，仍可在当前页面正常游玩”，关闭规则仍可继续。与 Playwright 实际 API 成功分别记录，不能把被拒绝说成成功。
- 回合制没有倒计时和独立暂停按钮；打开规则/记录会阻止操作底层棋盘，关闭后继续。电脑已开始的计算可在弹窗或后台完成一手；没有声称实现显式冻结电脑。刷新/退出重入以本机存档恢复，房间以 sessionStorage 恢复。后台及系统中断的真机生命周期仍待验收。

## 手机全屏发布矩阵

| 环境/入口 | 验证结果 | 可宣称的边界 |
| --- | --- | --- |
| Windows Playwright Chromium `140.0.7339.186` / 最新独立 `dist/` | 两棋盘 305px 进入完整文档全屏、对局、退出、旋转、继续、全屏中黑方结算重试通过 | 桌面引擎实际 Fullscreen API 加移动视口，**不是实体手机** |
| Windows 可见 Chrome CUA / 源码 43135 / 305px 视口 | 浏览器拒绝请求，准确反馈后仍可下棋 | 拒绝分支的实际当前浏览器证据 |
| Chromium API 缺失 / 拒绝注入 / 320px | 显示“不支持”/“未允许”，按钮保持非全屏且可继续落子 | 受控分支检查，非 Safari/Android 验收 |
| 父 Shell 同源 iframe | 已引入同一公共控制器；协调者负责最新整体集成检查 | 不用独立页结果替代 iframe 验收 |
| Android Chrome 实体机、iOS Safari 实体机 | **待验证** | 真实全屏或诚实降级、系统退出、工具栏、安全区、真实多点触摸均不能宣布通过 |
| 原生小游戏 | 当前无此发布实现 | 不适用，无平台验收声明 |

## 剩余项、上线条件与爆款实验

本轮三个已确认问题均有代码修复和回归，没有通过堆功能提升重玩分数。待独立评审继续查新缺陷；当前未发现新的确定严重阻断，但长期重复局面不自动判和仍是规则边界，15×15 细密交叉点是否易误触需要真人/真机判断。三档搜索在低端手机长局的实际耗时尚无证据。

**未达标，不能宣称整款游戏已全面上线验收通过。** 解除当前主要阻塞所需最少资源：一部 Android Chrome、一部 iPhone Safari；公开 HTTPS 前端及房间 API 的既有测试地址；两部手机跨网络邀约/断线试验；一组真实目标玩家首局及次日复玩观察。没有请求新付费服务，也没有部署本地服务到公网。

- **代码产品问题：** 本轮已修操作意图和抽子身份反馈；后续只根据独立复现的扣分项继续。不要先加签到/皮肤/分析平台。
- **体验假设：** 象棋走法与连五结合有辨识度，但新玩家能否把“吃将不胜/任意五子”与防守金圈学会尚未知。最小实验：请 5–8 名会一种棋的目标玩家无外部解释各玩首局，观察是否选对移动、能说出败因、主动开始第二局；次日复玩时看是否应用同一种防守策略。人数是建议招募规模，不是已有样本或统计结论。
- **增长假设：** 分享理由应是“邀好友玩熟悉棋子的陌生棋局”，适合朋友群/棋类短视频试玩入口。只用两种真实 10–20 秒对局片段比较用户是否进入首局、完成一局并自愿约朋友，复用现有统计/手工观察；没有数据不预测获客成本、留存或爆款概率。先验证需求再决定是否需要残局教学、复盘提示或更深联机服务。

---

# 历史：象五子棋上线体验改进 · 2026-09-21

## 本轮补充：相邻棋格触控误选修复

本节为本轮新检查；下文「已实现 / 验证证据」保留上一轮记录，不作为本轮新测试。独立复评仍由另一位 agent 完成，旧 `INDEPENDENT-REVIEW.md` 的 8.2 分不是本次自动验收结论。

### 发现与实现

实际触屏命中测试发现：棋子上方伸出的装饰 SVG 被 `.cell:not(:disabled) .piece-spirit { pointer-events:auto }` 开启点击；在 390 像素视口，格 40 上方的格 31 下缘坐标会命中格 40。玩家本来要向空位部署，却只选中了下一行棋子；15×15 小棋格也受影响。

删除上述覆盖，复用 `.piece-spirit` 已有的 `pointer-events:none`，让装饰保持显示、动画及随棋格悬停的效果，实际点击由该坐标所属棋格处理。没有改变规则、棋力、存档或房间协议，没有新增依赖。

### 本轮可复现证据

- `pnpm build`：静态产物生成成功。
- `pnpm test`：13/13 通过，含规则、电脑、存档与真实 HTTP 房间回归。
- `node browser-check.mjs`：使用父工作区已有 Playwright，临时启动纯静态 `dist/` 服务。修复前在 `320/xiangqi` 精准失败，点击后实际手数 `2`、预期 `3`；修复后 320、390、1280 宽 × 两种棋盘共 6 组全部通过，触摸空格下缘实际完成部署，刷新后位置/手数保留，无横向溢出。
- 同一浏览器检查使用 390×844、正常动画完成：默认单人首手、思考期间锁定输入、电脑完成且归还回合、刷新续局；无房间 API 时提示尚未开放并禁用创建；切同屏确认重开；实际交替九次落子完成红方五连、5 子高亮、结算「再来一局」清空；15×15 放大到 200% 后触摸部署，再恢复 100%。无未捕获页面异常。
- 复评可用源代码预览 `http://127.0.0.1:43101/`（包含本地房间 API）；静态房间降级应运行上述检查，其临时端口退出时关闭。

### 本轮前后估分

这是内部体验估分，不是真实用户均分；最终分数留给独立复评。

| 维度 | 权重 | 修复前 | 修复后建议 | 依据 |
| --- | ---: | ---: | ---: | --- |
| 核心玩法 | 30% | 8.4 | 8.4 | 已实际走通单人回应、同屏五连与重开，规则没有改变。 |
| 首局理解 | 20% | 8.3 | 8.3 | 直接部署、选子走法与连五目标已有提示，本轮没有扩大教学。 |
| 移动与反馈 | 20% | 6.8 | 8.3 | 已发现的相邻格误选会破坏玩家意图；修复后两棋盘三种宽度精准命中检查通过。 |
| 重玩价值 | 15% | 7.4 | 7.4 | 随机棋池、两棋盘和电脑难度保留，未用无关功能提高此项。 |
| 稳定性 | 15% | 8.1 | 8.4 | 触控误选根因消除且留下先失败后通过的回归；存档、重开、静态 Worker 流程重新实测。 |
| 加权总分 | 100% | **7.9** | **8.2（待独立复评）** | 精确值 7.865 → 8.21。 |

### 距离爆款仍缺什么

- 缺真实新玩家首局理解、首局完成率、第二局意愿和次日回访数据；专家评分不能推断用户均分或爆款概率。
- 优先用真人对局检验两档电脑的胜率与挑战感，再决定是否增加残局教学、复盘提示或更深棋力。
- 仍未自动判重复局面和棋；本轮没有引入会使旧合法棋谱无法重放的新规则。若实际对局重复拖局频繁，应连同旧存档迁移一起设计。
- 15×15 在窄屏仍需缩放，且本轮是浏览器触控模拟，未做实体手机、公网房间或跨运营商验收。

## 已实现

- 默认直接进入单人挑战（玩家执红），可选入门陪练、策略挑战，后者检查对方下一步五连。电脑计算放在 Web Worker，双方使用同一走法、随机棋池和胜负规则；思考期间禁止玩家替电脑落子。
- 保留双人同屏、两种棋盘和好友房间。切换模式/棋盘经过重开确认，重开会取消尚未完成的电脑计算。
- 本机每次有效行动和抽子后保存，以规则重放恢复棋局和待部署棋子；非法存档拒绝载入。进入好友局保留本机局，离开后恢复。
- 棋盘上方给出短教学，选子显示本棋种走法，金圈标出对手下一手成五的位置。已有移动/吃子动画、五连高亮和胜负后重开保留。
- 好友房间先检查服务是否可用；无 API 的静态版本禁用创建/加入并提供同屏入口。自建服务配置折叠，邀请链接直接填充房间和服务。

## 验证证据

- `pnpm test`：13 项通过。含两棋盘全部走法/原子操作/随机抽子锁定/吃子判胜、AI 直接取胜/防四连/无池移动/连续合法对局、存档精确重放与损坏拒绝、真实 HTTP 房间鉴权/版本冲突/双方重开/健康探测。
- `pnpm build`：通过，静态产物包含 Worker 及其全部依赖。
- CUA 浏览器实际操作：首手后 AI 回应且归还红方回合；非法兵走法不增手数；兵吃车；刷新续局；抽出未部署的兵刷新后仍为原兵；人类第 3 手后立即刷新，电脑完成且仅完成第 4 手。
- 同屏实际交替落子：红方第 7 手后显示五连威胁，第 9 手胜利并高亮 5 子；再来一局清空；切为 15×15 后右下角落子，刷新保留 225 个交叉点和第 1 手。
- 390×844 和 320×568 浏览器视口：无横向溢出，棋盘/模式/重开/缩放可操作；390 视口查看过完整棋盘截图。此为浏览器模拟，不是真机验收。
- 两个浏览器标签实际创建/加入好友房间，红黑各走一手并同步；退出回到之前第 1 手的本机棋局。
- 纯静态包：无房间 API 时显示“暂未开放在线房间”、创建按钮禁用；单人 Worker 正常回应。该次验证端口原为 4198，已确认 HTTP 标题和 Python 请求日志均属于象五子棋；为避免其他游戏冲突已迁至 **4188**，并重新核验标题。房间服务为 **4178**。

## 评分与边界

待独立评审按核心玩法 30%、上手 20%、移动与反馈 20%、复玩 15%、完成度稳定 15% 评分；实施者未将自身判断当作 8 分验收。

电脑是局势评分加有限候选防守检查，不是完整深度棋力搜索；没有重复局面自动判和。公网房间仍需实际部署 Node HTTPS 服务，未声称已经上线。尚未完成实体手机触控和跨运营商联机测试。未新增依赖，未提交或推送任何仓库。
