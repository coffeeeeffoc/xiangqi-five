# 2026-09-23 真实共享后端独立验收

评审者 `/root/review_kart_chess_backend` 未参与游戏或服务实现。先运行浏览器对局和真实 HTTP 请求，再阅读实现与历史报告；只修改评审记录及独立检查脚本。

| 项目 | 实际操作与证据 | 结论 |
| --- | --- | --- |
| 两个独立身份 PK | `node online-check.mjs` 创建两个独立浏览器 context，390×844 触控，点击创建/房间码加入/双方准备，Canvas 点击 9 手合法连五；两端查询相同结果、胜3负0 | 通过 H5 + PostgreSQL 链路 |
| 再来一局与防重复刷分 | 同两身份再赛、再准备、真实点击 9 手；第一局 rated=true，第二局 rated=false，总积分保持3/0 | 通过当日同对限额 |
| 聚焦与结果 | 棋盘、轮次、核心操作、退出/全屏/规则可见；结算显示双方有效成绩、累计积分、排名及参赛数 | 390×844 实际可用；不代表所有手机 |
| 全屏成功及失败 | 独立检查真正产生 `document.fullscreenElement`，调用系统退出后保留房间；另以跨源 iframe + `Permissions-Policy: fullscreen=()` 实际拒绝请求 | 失败反馈准确，仍可点击准备 |
| 身份与恢复 | 刷新后读取相同玩家 ID；打开 PK 自动恢复同一房间，没有再次添加成员 | 通过同浏览器恢复 |
| 真实邀请链接 | 点击“复制邀请”，从浏览器剪贴板读取生成的pk链接，再用新独立context打开并点击加入；身份不同、房间两人一致 | 通过H5链接邀请；房间码已在完整对局验证 |
| 非法/重复请求 | 实际 HTTP 错游戏、非成员、满房、错误轮次、越界/已占落点、伪造 score、seq 跳号、同seq异内容、重复准备及退出后操作 | 全部被拒绝或幂等；合法重传没有再走一步 |
| 既有单机/存档/AI回归 | 独立运行 `pnpm test` 20项通过，含战术电脑不送车、强制防守、抽子与规则、保存重放、旧HTTP房间；`node --test competition.test.mjs` 2项通过 | 规则与存档检查通过，不能代替所有设备试玩 |

运行入口：`http://127.0.0.1:43010/games/xiangqi-five/`，测试库 `competition_test`，未使用正式用户数据。浏览器证据：`.turbo/competition/online-result.json`、`online-round-1.png`、`online-round-2.png`。附加独立命令在父仓库执行 `node scripts/review-competition-kart-chess.mjs`，证据 `outputs/independent-kart-chess/review.json`、`xiangqi-fullscreen.png`、`xiangqi-fullscreen-denied.png`。

2026-09-23 公网追加复验：对实际临时 HTTPS 隧道以 `XIANGQI_COMPETITION_URL=<公网入口> node online-check.mjs` 重跑两轮，上述两个真实身份、准备、18次Canvas落子、一致胜负、3/0积分、第二局不重复计分均通过。新结果层第一次把旧“再来一局”盖在下层，初次脚本实际点击被拦截；总负责人已在结算层加入直接“再次挑战”，复用原再赛逻辑。检查更新为点击可见入口并等待新房间响应后重跑绿灯，没有强制点击遮挡元素。最新 `online-result.json` 是本次公网结果；临时域名只留被忽略证据目录。

本轮共享后端 Top100 的榜首负差距问题已由总负责人修复并经真实PG独立复验，详见 `services/runtime-api/COMPETITION-REVIEW.md`。微信/B站开发者工具、手机真机及跨平台 PK 本评审未实际运行，不能宣布通过。保留冻结目标加权≥9.5、每维≥9.0；本次专项链路检查不重新推高综合体验评分，既有完整评审 8.75 未达标，以下历史记录原样保留。

# 2026-09-22 独立评审 R3 · 冻结量表 v1

**结论：X3 通过独立复验，未发现本轮已验证路径的新可编码缺陷；仍未通过 9.5 门槛，加权原值 8.75。** 只将已修复的操作维度由 8.5 调为 9.0，其余维度沿用 R2。手机真机、真实目标玩家和公网房间证据仍缺失，桌面触控模拟不能替代这些条件。

## 独立性、版本与实际环境

- 评审者 `/root/review_chess_r3` 未参与实现。先读根 AGENTS 与冻结量表前 17 行，再独立核对 HTTP 字节、实际交互、记录截图和 `blind-notes.md`，完成 16 组缩放矩阵及 4 组核心循环后才读 R2 和 `RELEASE-REVIEW.md`。没有复用旧评审脚本，也没有改写旧证据。
- 固定入口为 `http://127.0.0.1:43143/small-games/#/games/xiangqi-five` 和 `http://127.0.0.1:43143/small-games/games/xiangqi-five/index.html`。未做资源请求覆写；使用正常 HTTP 制品、鼠标 `click()`、触摸 `tap()` 及真实 CDP 触摸取消，没有 `force`、棋局/胜负/随机数/存储注入。
- Windows Chrome **153.0.8010.50**；`chromium.launch({channel:'chrome',headless:false})`；每组隔离 context，`isMobile:false, hasTouch:true`。外层窗口 844×390 与 844×334；聚合入口游戏视口分别为 844×334、844×278。续局视口为 305×740。**本轮只有桌面 Chrome 实际 API 与触控模拟，不是 Android/iOS 手机真机。**
- 经典滚动条确实存在：独立 844px 页面 `clientWidth=829`；棋盘容器 15px 占位可从 `offsetWidth-clientWidth` 读到。每次点击放大后等待 650ms 读取，再等 550ms 再读，覆盖 ResizeObserver 与滚动条稳定后的状态。
- 11/11 游戏文件 HTTP 200，字节与 `review-build-r3` 及当前产品源码相同（`.nojekyll` 对照构建文件）；Shell HTML、公共全屏脚本及带指纹 JS/CSS 共 4/4 文件也与快照相同。`style.css` SHA-256 为 `14b167e61145a9d32da5cc8251aeb14232f572aee9a07b22e10e95647765efd9`。`app.js` 仍为 `cf63c2702bcf3ba2d79201c9ffdfcb1cc026f080fa1e2bd62ecfe9cc946c8884`；`board-view.js` 仍为 `ea79c1669a9e36b5ca3522269f3bfd690d83146d49a6137eb07b058ebfbe4c18`。
- 独立逐文件对比 R2/R3 的 11 个游戏制品，只有 `style.css` 改变。已读取根日志确认 `build:pages` 38/38、最新三入口集成 3/3；它们作为环境关联，独立交互结果另列。协调者提供的整树标识为 `80048623dd600b122cdcdd55edbd69ad9616cc590f3d0e3cff7fa565011f8ca2`，本评审另有上述逐文件 HTTP 证据。

## 逐维原值

| 维度 | 权重 | R2 → R3 | 本轮证据与边界 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 30% | 8.8 → 8.8 | 两入口、两棋盘正常九手红方五连、重开后继续落子通过；故意不防守的短棋谱只证明核心循环，不新增难度进阶、长局或目标玩家乐趣证据。 |
| 首局理解与教学 | 20% | 8.8 → 8.8 | 首屏规则、轮次、落子反馈可读；本轮只有 CSS 变化，未新增教学或真实新玩家迁移证据，沿用 R2。 |
| 移动操作、视听与反馈 | 20% | 8.5 → 9.0 | X3 所致占位滚动条下放大失效已关闭；16/16 双入口/双棋盘/双高度/鼠标与触摸组合准确落子、取消、旋转后重置/再放大、305px 与续局通过。9.0 仅限明示桌面/触控模拟范围，结合 R2 多点取消与根当前全屏降级证据；Android/iOS 真机、安全区/工具栏和声音听感仍未验。 |
| 重玩或持续学习 | 15% | 8.2 → 8.2 | 没有新增跨局/跨日真人、策略学习或真实复玩证据，不因测试数量或新评审而加分。 |
| 功能完整性与稳定性 | 15% | 8.8 → 8.8 | 当前完整存档、真实全屏进出、核心重开路径通过；X3 改善只计入操作维度。未改的本地房间沿用 R2 验收，未冒充本轮重跑；公网、真机系统后台和低端长局仍待验证。 |

原值：`8.8×0.30 + 8.8×0.20 + 9.0×0.20 + 8.2×0.15 + 8.8×0.15 = 8.75`。低于 9.5，且各维未全部达到 9；没有真实目标玩家样本，无法可靠估计用户均分。

## X3 通过证据及兄弟路径

1. **16/16 实际矩阵：** 独立/聚合 × 9×10/15×15 × 外层 844×390/844×334 × 鼠标/触摸，全部从 100% 持续升到 150%、200%、250%、300%。每档两次稳定读取倍率与棋盘实际几何，未出现 150% 回落 100% 或棋盘自行缩回。每档真实点 A1、C3、E5、G7、I9 中的对应一格，手数依次 1–5，目标格身份正确。共 80 档稳定检查、96 次矩阵真实落子。
2. **300% 后的取消：** 真实 CDP `touchStart→touchMove→touchCancel` 不新增手数或改变棋子标签；随后仍能继续操作。本轮是单指移动取消，R2 双指取消证据保留，不扩称本轮重跑了所有多点手势。
3. **整页滚动条兄弟路径：** 每组从 300% 改为 305×740，倍率正确重置 100%；再次放大可稳定在 150%；恢复大小后准确点 B2，手数变 6；刷新恢复全部六个棋子标签与手数。所有 305px 记录中 `scrollWidth <= clientWidth`，无横向页面溢出。
4. **真实全屏：** 两入口 × 两棋盘共四组实际点击进入/退出；独立 `document.fullscreenElement=HTML`，聚合顶层为 `MAIN`；退出后为 null，六手棋局保留。全屏 API 未被模拟替换。根最新不支持入口集成的降级检查为补充证据，不算手机 Safari 验收。
5. **4/4 核心五连/重开：** 两入口 × 两棋盘交替真实点击/触摸 `A1,A10,B1,C10,C1,E10,D1,G10,E1`，九手后红方五连；点“再来一局”归零，再触摸 E5 得到第 1 手。未注入胜负，故意不防守不能证明 AI 棋力或真人平衡。16 组矩阵及 4 组核心循环的 `pageerror` 都为空。

下表是鼠标用例实测棋盘宽度（CSS px），触摸相同配置亦通过；它记录实际可操作内容尺寸，不假设边框/留白按严格比例一起放大。

| 入口 / 棋盘 / 外层视口 | 100% | 150% | 200% | 250% | 300% |
| --- | ---: | ---: | ---: | ---: | ---: |
| 独立 / 9×10 / 844×390 | 215.1875 | 332.5 | 450 | 567.5 | 685 |
| 独立 / 9×10 / 844×334 | 164.796875 | 257.5 | 350 | 442.5 | 535 |
| 独立 / 15×15 / 844×390 | 243 | 374.5 | 506 | 637.5 | 769 |
| 独立 / 15×15 / 844×334 | 187 | 290.5 | 394 | 497.5 | 601 |
| 聚合 / 9×10 / 844×390 | 164.796875 | 257.5 | 350 | 442.5 | 535 |
| 聚合 / 9×10 / 844×334 | 114.390625 | 181 | 248 | 315 | 382 |
| 聚合 / 15×15 / 844×390 | 187 | 290.5 | 394 | 497.5 | 601 |
| 聚合 / 15×15 / 844×334 | 131 | 206.5 | 282 | 357.5 | 433 |

先完成上述体验后再对照 R2 与实现记录：仅 CSS 稳定棋盘及根页面滚动条占位，实际修复了 R2 的 `clientWidth` 抖动场景；原 `app.js`、`board-view.js` 字节未变。X1 的横屏总览与 X2 的本机房间中文恢复沿用 R2 已确认结果。本轮没有新缺陷需要增加玩法或架构来解决。

## 证据路径、重跑与剩余门槛

全部新证据只在**根仓库** `.scratch/six-games-95/review-chess-r3/`：

- `blind-notes.md`：读取旧报告前封存的观察；`http-manifest.json`、`http-shell-manifest.json`、`environment.json`：HTTP/快照/源码与 Chrome 版本。
- `matrix.mjs` / `matrix-results.json`：16 组全链路。重跑 `node .scratch/six-games-95/review-chess-r3/matrix.mjs`。
- `core.mjs` / `core-results.json`：4 组真实胜利与重开。重跑 `node .scratch/six-games-95/review-chess-r3/core.mjs`。
- `*-300.png`、`*-305.png`、`core-*-win.png`：实测截图。早期 `pilot-results.json` 的严格像素比例假设、`pilot-cancel-results.json` 的动画期 DOM 属性比较均被识别为测试断言问题，已保留；最终比较稳定实际尺寸及棋局/手数，16 组全部重跑通过，没有修改产品来满足测试。

**仍不通过 9.5。** 剩余是已明示的实际资源验证：Android Chrome 与 iPhone Safari 的完整全屏或诚实降级、真实工具栏/安全区/系统后台及低端长局；目标玩家无外部解释首局迁移和次日复玩；真实 HTTPS 前端/API、两机跨网络邀请/断网/恢复/席位。未验不算通过，也不能从桌面代理自评分推断真实用户评分。没有提交、推送或部署；只更新本独立报告及自有 scratch 证据。

---

# 2026-09-22 独立评审 R2 · 冻结量表 v1

**结论：未通过 9.5 门槛，加权原值 8.65。X2 中文断网恢复通过；X1 横屏总览改善，但新发现 X3：有占位滚动条的可见 Chrome 中，放大棋盘立即退回 100%，仍需代码修复。** 未发现本轮已操作路径丢失棋局；不能把移动视口模拟、代理短局或本机房间当实体手机、真实玩家或公网验收。

## 独立性与固定版本

- 评审者 `/root/review_chess_r2` 未参与实现。先读冻结量表、实际读规则并用正常交替触摸完成九手五连、重开和两棋盘横屏观察，之后才读下方 R1 与 `RELEASE-REVIEW.md`。未注入棋局、随机数或胜负。
- 固定入口 `http://127.0.0.1:43142/small-games/#/games/xiangqi-five` 与 `/small-games/games/xiangqi-five/index.html`；未修改 `.scratch/six-games-95/review-build-r2`，未提交、推送、部署。
- Windows Chrome **153.0.8010.50**，隔离新进程/浏览器 context；844×390、305×740。移动触摸模拟与有头桌面响应式窗口分开记录，均不是 Android/iOS 真机。
- 关键文件实际 HTTP 200 字节与固定快照逐一 SHA-256 一致：HTML `045a7a43c475c38e793bf5493d246408793f1d1ae9081519a536a3ec722e4ebe`；app.js `cf63c2702bcf3ba2d79201c9ffdfcb1cc026f080fa1e2bd62ecfe9cc946c8884`；style.css `25fd1de526ee3138a57cb3b511f1e869f3271191fc1021324cb80bd94752fc85`；board-view.js `ea79c1669a9e36b5ca3522269f3bfd690d83146d49a6137eb07b058ebfbe4c18`；fullscreen.js `4451eded9653669ae98fd27e9dc3eff43d41c14286ec66a27d99629f18a34828`。完整七文件记录见 `review-result.json`。
- 证据均在**根仓库** `.scratch/six-games-95/review-chess-r2/`。主检查 `node .scratch/six-games-95/review-chess-r2/review.mjs`；X3 最小复现 `node .scratch/six-games-95/review-chess-r2/zoom-headed.mjs`。未重复无关构建，根协调者的整体构建/入口检查与本轮独立交互分别记录。

## 评分变化

只给已确认改善加分；未修改维度沿用 R1，不在独立评审间择高或平均。

| 维度 | 权重 | R1 → R2 | 本轮依据与边界 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 30% | 8.8 → 8.8 | 独立真实九手五连、重开通过；故意不防守，只验证核心循环，不新增 AI 难度、早中后盘或真人乐趣证据。 |
| 首局理解与教学 | 20% | 8.7 → 8.8 | 原断网英文异常变为明确中文中断/重试/恢复反馈，已实际验证；教学本身未新增，目标玩家迁移仍待验。 |
| 移动操作、视听与反馈 | 20% | 8.1 → 8.5 | 两入口、两棋盘横屏可以总览；移动模拟 300% 准确输入、取消、转屏恢复通过。X3 导致有占位滚动条的桌面窗口不能保持放大，尚不能达到关键环境无障碍的 9 分。真机与声音听感未验。 |
| 重玩或持续学习 | 15% | 8.2 → 8.2 | 本轮没有新增跨局/跨日真人或策略学习证据。 |
| 功能完整性与稳定性 | 15% | 8.8 → 8.8 | 当前输入、刷新续局和本机双客户端恢复可用；新增 X3 在操作维度扣分。无新增完整公网发布、真实系统后台/低端长局证据。 |

原值：`8.8×0.30 + 8.8×0.20 + 8.5×0.20 + 8.2×0.15 + 8.8×0.15 = 8.65`。低于 9.5，且各维没有全部达到 9；不能可靠估计上线用户均分。

## 实际输入与整改验收

1. **核心循环：** 隔离浏览器通过正常触摸 `A5,A1,B5,B1,C5,C1,D5,D1,E5`，第九手红方五连，结算后实际点击“再来一局”回到 0 手。`isolated-win.png`、`review-result.json`。双方有意不防守，不是电脑获胜率/真人对弈质量证据。
2. **X1 总览改善：** 独立 9×10 约 230.19×255.75、15×15 为 258×258，顶端 y=58；聚合分别约 179.80×199.77、202×202，顶端 y=114，全部在外层 390px 内。`direct-*-overview.png`、`shell-*-overview.png`。
3. **移动视口回归：** 四个入口/棋盘组合都实际触摸首末格，再由按钮升到 300%，中部 E5/H8 准确落子；双指开始/移动/`touchCancel` 不新增手数。等待取消抑制结束，转为 305×740，B1 继续落子到四手，刷新后全部棋子标签相同，文档宽和滚动宽均 305。见 `report.json`、`*-300.png`、`*-305-restored.png`。这不能覆盖下述有头桌面滚动条失败。
4. **X2 通过：** 当前固定前端连接临时仅 `127.0.0.1` 的真实 Node 房间 API，两个隔离 context 正常创建/加入，各下一手。红方浏览器 context 断网后，中文状态“连接中断，正在重试同步。”且全部棋格锁定；恢复后显示“连接已恢复，棋局已同步。”并继续第三手。黑方刷新仍执黑、保留三手，继续第四手，两边所有棋子标签一致。`report-room.json`、`room-offline.png`、`room-recovered-message.png`、`room-seat-restored-continue.png`。不是公网或两台手机。
5. **证据修正透明记录：** 初始房间测试夹具错误地等待落子方在换手后立即解锁，造成超时；只修测试等待，单独重跑房间通过，原 `report.json` 保留，汇总 `review-result.json` 指向修正结果。共享 CUA 中间缩放过程不作关键证据；X3 已由下述独立进程和鼠标/触摸输入重新确认，不能归为工具污染。

## 新缺陷 X3 · 横屏放大被滚动条触发的重置撤销（中等，待修）

- **最小复现：** Windows 可见 Chrome，`chromium.launch({channel:'chrome',headless:false})`，新 context/page `{viewport:{width:844,height:390},hasTouch:true,isMobile:false}`；43142 独立入口，切双人同屏、15×15，空盘即能复现。鼠标点击“放大棋盘”四次，每次 `100% → 150% → 100%`，不能稳定到 300%。另以真实 `tap()` 复现相同失败。
- **根因证据：** `zoom-headed.json` 逐动画帧读取 DOM：初始 `clientWidth=278, offsetWidth=278, clientHeight=292, scrollHeight=292`；150% 时 `clientWidth=263, offsetWidth=278, clientHeight=238, scrollHeight=431`；随后回到 100%。垂直滚动条占 15px，外宽未变；`style.css:126` 的短横屏 `scrollbar-gutter:auto` 与 `board-view.js:62` 按 `clientWidth` 判断的 `ResizeObserver.reset()` 相互作用。`zoom-reset-headed.png` 与 `report-headed.json` 支持复现。
- **玩家影响：** 横屏 15×15 总览格很小，玩家需要放大才能精确操作，但经典占位滚动条环境放大失效。不是棋局丢失，也不能因无头移动模拟通过就宣称修复完成。
- **通过条件：** 新固定制品中，经典滚动条的有头 Chrome 下独立/聚合、两棋盘都能到 300% 并保持，真实鼠标/触摸落在目标格；旋转及全屏等真正的可用尺寸变化仍正确重置，取消和刷新续局继续通过。43142 保持原样供红灯对照，修后另作 R3。

## 仍待实际资源验证

先修 X3。除此之外，本轮没有证据要求加新玩法、奖励或教学体系；真实目标玩家首局/次日复玩、Android Chrome 与 iPhone Safari 的真实全屏或诚实降级/工具栏/安全区/系统后台，以及真实 HTTPS 前端/API 两机跨网络邀请、失联恢复和席位验收仍待完成。桌面代理和本机双页通过不能填补这些缺口，也不能宣布达到 9.5。

---

# 2026-09-22 独立评审 R1 · 冻结量表 v1

**结论：未通过9.5门槛。独立加权原值8.55；两个可复现改进项为横屏无法适屏看完整棋盘、房间断网显示英文底层错误。未发现当前所玩路径的数据丢失或严重阻断。** 本报告固定在43141制品，后续修复须另建验证记录。

## 独立性、版本与证据

先只读冻结量表与专项检查，自由实际落子、尝试移动和电脑对弈，记录胜负及布局后才核对RELEASE-REVIEW.md。未参与实现、未运行实现者browser-check代替自由体验。双人九手五连和故意忽略威胁是独立的受控棋谱，不能证明真人胜率、平衡或乐趣。

- 根起点 `eeb877d75df163a27287162c7d48f27f6cbc8184`；独立子模块起点 `bd8f8b7e079e6b61c6634cf4a0c0730d43765cf4`，均加冻结时未提交变更。
- 聚合 `http://127.0.0.1:43141/small-games/#/games/xiangqi-five`；独立 `/small-games/games/xiangqi-five/index.html`。入口HTTP字节与固定制品匹配。
- SHA-256：HTML `045a7a43c475c38e793bf5493d246408793f1d1ae9081519a536a3ec722e4ebe`；app.js `847c32a581e63f35b3075493a4009aa383529309a89ca9614427359b4f08e795`；board-view.js `ea79c1669a9e36b5ca3522269f3bfd690d83146d49a6137eb07b058ebfbe4c18`；fullscreen.js `4451eded9653669ae98fd27e9dc3eff43d41c14286ec66a27d99629f18a34828`。
- Windows Chromium `140.0.7339.186`，390×844、305×740、844×390，移动viewport/触控模拟。实际桌面Fullscreen API成功；API拒绝注入只代表受控降级分支。
- 证据目录在**根仓库** `.scratch/six-games-95/review-history-chess-r1/`；重跑 `node .scratch/six-games-95/review-history-chess-r1/review-check.mjs`。记录 `review-check.json`、`review-observations.json`。
- 房间使用冻结前端及临时仅本机 `127.0.0.1:43243` 服务，两个隔离context；服务配置只允许43141来源。不是公网、跨运营商或两台手机验收。评审只写本报告和scratch证据，未修改产品源码、提交、推送或部署。

## 本轮新独立基线

未实跑实现前快照，以下不宣称历史增分；当前行为证据支持的档位就是独立新基线。测试数量不算玩法或复玩提升。

| 维度 | 权重 | 分数 | 观察到的表现与扣分边界 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 30% | 8.8 | 连线、抽子、移动吃子与电脑反吃产生真实攻防；金圈提示迫使玩家在堵点/吃子间选择。仅玩标准电脑和同屏短局，尚无早中后盘持续质量、三档难度进阶或目标玩家乐趣证据。 |
| 首局理解与教学 | 20% | 8.7 | 主界面短句、选子走法、合法点与非法提示能帮助修正；规则说明连五、吃将不胜和兵可后退。未有首次目标玩家理解/迁移观察；断网英文错误不利于理解恢复状态。 |
| 移动操作、视听与反馈 | 20% | 8.1 | 305px竖屏可玩，15×15可放大至300%，多点取消不吞后续落子；全屏/退出保留棋局。844×390棋盘808～898px高、无法缩至100%以下，完整局面不能同时看见，影响横屏决策。真机和声音听感未验收。 |
| 重玩或持续学习 | 15% | 8.2 | 两棋盘、随机棋种、电脑难度和好友房存在变化，失败后能重试并查看记录。只有短局代理体验；暂无真实跨局/跨日复玩、学会防五/反吃或长局重复局面证据。 |
| 功能完整性与稳定性 | 15% | 8.8 | 单人回应、合法性、胜负/重开、抽子锁定、刷新、房间同步/断网锁定/恢复/退出还原可用。缺真机低性能长局、系统后台和最终公网发布/房间链路证据。 |

原值：`8.8×0.30 + 8.7×0.20 + 8.1×0.20 + 8.2×0.15 + 8.8×0.15 = 8.55`。未满足总分>=9.5、各维>=9及关键声明环境真实证据门槛；无法可靠估计用户均分。

## 自由实际对弈及异常记录

1. 聚合390px默认标准电脑：E5落红士；点击士后绿点显示斜一步，误点E4提示不能走，D4合法移动。刷新前后D4红士、E6黑车、F6黑卒一致。
2. 继续在E4、F4部署，黑方在第8手形成C6～F6四连；金圈显示B6/G6。红车F4吃F6黑卒解除连线，但被E6黑车反吃；这是当前实玩中明确可理解的攻防。随后故意A1/A2落子忽略威胁，第14手黑胜。证据 `chess-midgame.png`、`chess-loss.png`、`chess-natural-loss.json`。
3. 点击再来一局，切15×15与双人同屏；实际交替A1/A15/B1/C15/C1/E15/D1/G15/E1，第9手红胜。重开对话框选继续，胜局保留。证据 `chess-win-305.png`。
4. 305px棋格初始约18.19px，放大到300%后约56.72px，页面无横溢出。CDP真实触控协议双指开始、移动、touchCancel后记录仍0手；等待550ms后tap H8变1手。不是给游戏注入棋局。证据 `chess-gomoku-305.png`、`chess-gomoku-zoom-305.png`、`chess-touch-cancel-followup.png`。
5. 独立检查复现“移动模式误点空位”：手数及localStorage字节不变并显示先选己方棋子。快速双击抽子只得到一个待部署棋种，刷新仍显示相同棋种。见 `review-check.mjs/json`；该项核实实现报告的修复当前有效，不额外推断分数提升。
6. 聚合全屏为Shell MAIN，独立为HTML；规则弹窗可退出全屏，棋局仍1手。实际全屏和注入拒绝分别记录；拒绝时有中文诚实反馈。
7. 纯静态版本房间入口诚实说明未开放并提供同屏。接本机服务后双方各落1手、2手一致；黑方断网按钮锁定，恢复/刷新仍2手；红方申请重开，黑方取消后保留2手；红方离房恢复入房前本机1手。证据 `chess-room-recovered.png` 及操作记录。回合制无独立暂停倒计时；不能将弹窗叫作冻结电脑思考。

## 可代码修复的问题

### X1 · 横屏不能看完整局面（中等，移动维度）

- **步骤：** 43141聚合或独立入口，设844×390，15×15；恢复大小到100%。独立棋盘808×808，顶部y=295、最下格y=1049；聚合还增加56px Shell栏，首屏只露棋盘约45px。9×10独立检查同样得到808×898。缩小按钮在100%禁用。
- **玩家影响：** 玩家必须来回滚动看两端威胁，无法同时判断完整连线；触点虽大，但旋转后丢失完整局势。不是横向溢出或数据损坏，未列严重阻断。
- **证据：** `chess-embedded-landscape.png`、`chess-standalone-landscape.png`、`chess-xiangqi-landscape.png`、`review-check.json`。CodeGraph未索引该子模块后读取当前 `board-view.js`，缩放下限为1，默认尺寸以宽度为基准。
- **通过条件：** 两种棋盘在独立/聚合844×390可缩至适屏看完整局势，仍能放大准确落子；至少提供明确适屏或竖屏引导而非默默截断。305×740竖屏棋盘/控件不溢出，300%缩放、双指取消后落子、刷新存档不回归。

### X2 · 房间断网露出英文底层异常（轻中，教学/反馈维度）

- **步骤：** 冻结前端连接本机43243，两隔离context入房各落1手；对黑方浏览器context设离线，约1.2秒。
- **实际：** 状态为“房间码 · 你执黑 · Failed to fetch”，走子正确禁用；恢复网络后正常同步。玩家没有得到明确中文的恢复解释。
- **证据：** `chess-room-offline-english.png`、`chess-room-offline.txt`。当前 `renderRoom()` 优先展示`roomError`，网络请求的原始错误message流入玩家文案。
- **通过条件：** 请求不可达显示中文“连接中断，正在重试同步”等准确状态，保留操作锁定；恢复后手数/席位不变。服务有意义的业务错误仍可区分，无需包装新网络框架。

## 体验假设与外部瓶颈

- **玩法/教学/重玩证据不足：** 复现观察任务是让未玩过的人先实际抽子、移动、看金圈，完成一局后说明败因，并次日再玩。影响是目前无法判断“吃将不胜”“随机棋种+连五”是否被快速理解，以及是否带来持续策略学习。当前没有证据要求添加残局课或新奖励系统；先观察再修。
- **真实设备：** Android目标浏览器、iPhone Safari应分别完成两棋盘305等效窄屏/旋转、多触点取消、软键盘/工具栏、完整全屏或诚实降级、系统后台恢复。低端机长局和电脑最慢响应需实测，桌面Worker回应不能替代。
- **公网房间：** 需要实际HTTPS前端/API、两台设备跨网络邀请、失联恢复和席位语义验收；本机API成功不等同公网。未请求部署或付费服务。
- 本轮先修X1/X2并针对固定新制品复验，再补外部证据；不能靠增加测试数量或修改量表直接抬到9.5。

---

# 2026-09-21 历史第二轮独立复评（非本轮结论）

2026-09-21。评审代理 `/root/review_words_chess_round2` 未参与产品实现。本轮检查当前源码，重新构建和运行检查，并使用 CUA 亲自操作 Chrome。人工预览为 `http://127.0.0.1:43101/`（源码及本地房间 API）；自动浏览器回归独立启动纯静态 `dist/` 服务。

**内部体验评分 8.1 / 10（加权原值 8.095），通过单机 / 同屏浏览器首发体验门槛。** 各维均不低于 6，未发现尚未解决的严重玩法阻断。此分数不是用户均分，不代表公网好友房间或实体手机已验收。

| 维度 | 权重 | 本轮分数 | 当前证据与扣分 |
| --- | ---: | ---: | --- |
| 核心玩法 | 30% | 8.3 | 实际完成随机部署、电脑回应、选相查看走法、非法吃子拒绝、合法移动及同屏九手五连；象棋走法与连五目标有相互作用。重复局面仍无自动和棋。 |
| 首局理解 | 20% | 8.1 | 默认单人直接落子，首屏写清「点空位随机上场 / 点己方棋子移动 / 同色连五」；选相后解释斜走两格，四连后有金圈防守提醒。尚缺真人首局学习数据与分步残局教学。 |
| 移动端操作与反馈 | 20% | 8.0 | 390×844 主棋盘与操作可见，320×740 胜利和重开可用；邻格下缘实际落在正确位置，15×15 放大 200% 后可落子。小屏大棋盘仍依赖缩放，未测试实体触摸误差。 |
| 重玩价值 | 15% | 7.5 | 两种棋盘、两档电脑、同屏与随机棋池提供变化；当前电脑为有限候选局势评分，没有更深成长层级、残局目标或复盘解释。 |
| 完整性与稳定性 | 15% | 8.4 | 13 项规则 / 电脑 / 存档 / 本地 HTTP 房间检查及构建通过；静态 Worker、六组邻格触控、刷新、无 API 降级、胜利重开回归通过。公网联机和长局性能仍未覆盖。 |

## 本轮 CUA 亲自操作证据

- 390×844 默认策略挑战，在 E5 部署红相；电脑思考时棋盘锁定，电脑在 E6 落黑车后归还红方。刷新保持两手棋局并显示续局说明。
- 选择 E5 红相后，界面显示「斜走两格，中间有子不能走」，C3/G3/C7/G7 显示可移动。尝试吃相邻 E6 黑车被明确拒绝，手数不变；随后实际移到 C3 并触发电脑下一手。
- 通过正常模式选择与重开确认进入同屏，以 `A1, I10, B1, I8, C1, I6, D1, I4, E1` 真实交替部署。第七手后 E1 标记对方下一手成五并解释防守；第九手显示红方获胜、五子连线和再来一局。320×740 截图确认五枚高亮及可见重开按钮；实际重开后记录归零。该棋谱故意不防守，仅验证胜利闭环，不证明电脑难度或真人获胜率。
- 本轮修复的独立命中验证：320×740 下先在 E5 部署红兵、I10 部署黑车，再点 E4 的下缘坐标（DOM 实测约 x=145、y=414）。页面实际在 E4 部署红炮，对局记录由 2 变 3，未误选 E5 棋子；没有修改随机数或游戏状态。
- 正常切换 15×15 并确认重开，通过放大按钮到 200%，实际在 E5 部署红帅、记录变为 1。临时视口覆盖在检查后已恢复。所用页面 console error 为空。

## 本轮重新运行的检查

在本游戏目录执行 `pnpm test`、`pnpm build`、`node browser-check.mjs`，全部成功。13 项测试覆盖规则原子性、移动吃子、五连、有限电脑防守、合法存档重放与真实本地 HTTP 房间鉴权 / 版本冲突 / 双方重开。

浏览器回归自启纯静态 `dist/` 服务，验证 320、390、1280 宽 × 9×10、15×15 两棋盘的邻格下缘真实触控、落子、刷新和无横向溢出；另在 390×844 检查电脑 Worker 回应 / 思考锁定、静态无 API 时禁用房间创建并提示暂未开放、九手五连、五子高亮、胜利重开及放大触控。

脚本固定随机数只用于可复现命中 / 状态回归；独立 CUA 操作保留正常随机棋子。本轮没有将自动棋谱或一次成功对局当作长期平衡证据。

## 上线边界与爆款优先项

当前没有新增阻断修复要求。此次评分范围为单机 / 同屏浏览器；公开 Pages / Shell iframe、实体手机、长局低端设备、公网 HTTPS 房间和跨运营商同步仍需另外验收。房间 HTTP 单测成功不等于公网联机上线。

1. 找真实新玩家记录首局理解、平均步数、胜负与第二局意愿；根据「不知道如何防守」的频率先做少量残局教学与复盘解释。
2. 以真人对局分布校准两档电脑和长局重复走子的发生率，再决定加深搜索或引入重复局面和棋及存档迁移。
3. 先验证单机留存和好友约战的实际需求；公开房间需要真实部署、移动网络恢复及分享加入全流程证据。

---

# 历史归档：第一轮独立复评

以下保留旧报告原文和旧分数；历史地址与验证结果不能替代本轮复评。

# 象五子棋独立复评

日期：2026-09-21。评审者不是本轮实现者。对象：当前静态构建 `http://127.0.0.1:4188/`。这是单机及同屏首发体验的专家评估，不是真实用户平均分。

## 评分

| 维度 | 权重 | 分数 | 依据 |
| --- | ---: | ---: | --- |
| 核心玩法与主动操作 | 30% | 8.4 | 独立实际完成部署、选子、合法移动、吃子和电脑回应；象棋走法与五连目标有明确相互作用，同屏真实九手五连进入胜利结算。 |
| 首局清晰度 | 20% | 8.3 | 默认单人可直接落子，说明明确“随机上场/移动/同色连五”；选相显示斜走两格与塞象眼规则，非法吃子给出原位提示，四连出现可见金圈和防守解释。 |
| 移动端操作与反馈 | 20% | 8.2 | 390×844 下棋盘、模式、抽子与重开均可用，落子/吃子有动画，合法落点可见；320×568 胜利页可操作且无横向溢出。小屏大棋盘仍需要缩放，属于可用但尚可打磨的部分。 |
| 重玩与学习价值 | 15% | 7.4 | 两档电脑、双人同屏、两种棋盘和随机棋池提供复玩基础；没有残局、复盘讲解或更深棋力层级，长期学习回报尚有限。 |
| 完整度与稳定性 | 15% | 8.4 | 独立刷新后准确恢复棋局及手数，切模式先确认，胜后重开清空；静态无服务时禁用在线房间并说明替代玩法。独立重跑 13 项规则/AI/存档/HTTP 测试全部通过。 |

加权：`8.4×0.30 + 8.3×0.20 + 8.2×0.20 + 7.4×0.15 + 8.4×0.15 = 8.19`，展示为 **8.2/10，通过本轮 8 分门槛**。各维度高于 6，未发现阻断单机或同屏首发的缺陷。

## 独立实际试玩

1. 390×844 开始默认策略挑战，在 E5 随机部署红相；电脑在 E6 部署黑卒，回到红方第 3 手。
2. 选 E5 红相，界面显示象的走法及四个合法绿点。点击 E6 黑卒被拒绝，手数仍为 3；点击 C3 合法位置完成移动。
3. 电脑在 E5 部署黑将。刷新后显示“已续上次棋局”，C3 红相、E5 黑将、E6 黑卒及第 5 手均保留。随后红相从 C3 吃 E5 黑将，正常进入电脑回合，未错误判为将死获胜。
4. 打开好友房间：明确提示“此版本暂未开放在线房间”，创建/加入均禁用，仍可继续单机或同屏。没有把本地 API 等同公网服务。
5. 切换双人同屏出现重开确认。实际依次落子 A1、I10、B1、H9、C1、G8、D1，出现“对方下一手可成五”金圈 E1；再落 F7、E1，红方第 9 手五连获胜。点击“再来一局”恢复第 1 手和空棋盘。
6. 查看 320×568 的胜利界面及重开入口，DOM 横向宽度未超过视口；在 390 宽完整观察过棋盘、绿点和金圈截图。
7. 独立执行 `pnpm test`：13/13 通过，涵盖两种棋盘、全部棋子走法、吃子判胜、AI 合法行动/防守、存档重放与真实 HTTP 房间授权及版本检查。

## 非阻断优化余地与未测边界

- 策略电脑是有限候选的局面评分，能形成完整单人对局；尚不能把“策略挑战”宣传成专业棋力。优先根据真人胜率调整两档强度，再考虑残局或落子解释。
- 没有重复局面自动判和；长期反复移动可能拖长一局。若真人对局证实常见，可补重复局面或双方同意和棋。
- 本轮独立浏览器未重跑完整双标签好友房间，也未使用实体手机。HTTP 测试和实现者双标签证据不等于公网、跨运营商或弱网验收；这些仍是在线版发布前的独立事项。
